const { addPricePlans, updatePricePlansById,
    removePricePlans, getPricePlans,
    } = require('./priceplans.service')

module.exports = { addPricePlans, updatePricePlansById, removePricePlans, getPricePlans}